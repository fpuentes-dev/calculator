import Home from '@src/views/Home'

export const metadata = {
  title: 'Calculator',
  description: 'Calculate every operation in the same app',
}

export default function Page() {
  return <Home />
}
